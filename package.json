{
  "name": "hook-master",
  "version": "1.0.1",
  "description": "Create, remove and trigger synchronous or asynchronous events easily.",
  "main": "src/hook-master.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha test/*.spec.js",
    "docs": "javadoc -i src/hook-master.js -o README.md -f markdown",
    "coverage": "nyc --reporter=html ./node_modules/.bin/mocha test/*.spec.js",
    "clean": "rimraf node_modules && rimraf coverage && rimraf .nyc_output"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "javadoc": "^1.0.6",
    "mocha": "^6.1.4",
    "nyc": "^14.0.0",
    "rimraf": "^2.6.3"
  }
}
